{
  "name": "radar",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev --workspace @radar/server\" \"npm run dev --workspace @radar/web\"",
    "dev:server": "npm run dev --workspace @radar/server",
    "dev:web": "npm run dev --workspace @radar/web",
    "seed": "npm run seed --workspace @radar/server",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspace @radar/server && npm run typecheck --workspace @radar/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
