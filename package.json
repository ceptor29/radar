{
  "name": "aegis-grc",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev --workspace @aegis/server\" \"npm run dev --workspace @aegis/web\"",
    "dev:server": "npm run dev --workspace @aegis/server",
    "dev:web": "npm run dev --workspace @aegis/web",
    "seed": "npm run seed --workspace @aegis/server",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspace @aegis/server && npm run typecheck --workspace @aegis/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
